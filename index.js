import { registerRootComponent } from "expo";
import App from "./App";

// registerRootComponent ensures the environment is set up
// correctly whether running in Expo Go or a native build.
registerRootComponent(App);
